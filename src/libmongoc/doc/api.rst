API Reference
=============

.. Grab all pages that start `mongoc_` and end `_t`, which will recursively index more pages
.. toctree::
   :titlesonly:
   :maxdepth: 1
   :glob:

   init-cleanup
   logging
   errors
   lifecycle
   gridfs
   mongoc_rand
   mongoc_version

   mongoc_*_t
