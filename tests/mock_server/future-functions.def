#ifdef __clang__
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wunused-function"
#endif

FUTURE_FUNCTION (uint32_t,
                 future_bulk_operation_execute,
                 mongoc_bulk_operation_execute,
                 FUTURE_PARAM(mongoc_bulk_operation_ptr, bulk),
                 FUTURE_PARAM(bson_ptr,                  reply),
                 FUTURE_PARAM(bson_error_ptr,            error))

FUTURE_FUNCTION (bool,
                 future_cursor_next,
                 mongoc_cursor_next,
                 FUTURE_PARAM(mongoc_cursor_ptr,         cursor),
                 FUTURE_PARAM(const_bson_ptr_ptr,        doc))

FUTURE_FUNCTION (char_ptr_ptr,
                 future_client_get_database_names,
                 mongoc_client_get_database_names,
                 FUTURE_PARAM(mongoc_client_ptr,         client),
                 FUTURE_PARAM(bson_error_ptr,            error))

FUTURE_FUNCTION (char_ptr_ptr,
                 future_database_get_collection_names,
                 mongoc_database_get_collection_names,
                 FUTURE_PARAM(mongoc_database_ptr,       database),
                 FUTURE_PARAM(bson_error_ptr,            error))

#ifdef __clang__
#pragma clang diagnostic pop
#endif
